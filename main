#include <stdio.h>
#include <stdlib.h>

void display();

typedef struct _node
{
    void* data;
    struct node* prev;
    struct node* next;
}_NODE;

typedef struct _llist
{
    _NODE* head;
    _NODE* tail;
    int count;
} LLIST;



int main() 
{  
    struct _node* head;
    struct _node* tail;
    struct _node* temp;

return 0;
}
